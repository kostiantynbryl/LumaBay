# Third-party content

Version 0.1 Alpha contains no downloaded stock textures, characters, music or sound effects.

Visuals are generated at runtime by `ProceduralArt.cs`, and sound effects are synthesized at runtime by `AudioSynth.cs`. This avoids unclear asset licenses during the prototype stage.

Unity packages are resolved through the Unity Package Manager and remain governed by Unity's package licenses.
