using UnityEngine;

namespace LumaBay
{
    // Legacy V2 composition pass retained only for source compatibility.
    // It must not install or modify runtime UI; Stable Interface V6 is authoritative.
    public sealed class LumaBayPremiumCompositionV2 : MonoBehaviour
    {
    }
}
